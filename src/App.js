// Comment out the import not being tested
// import { API } from 'aws-amplify'
import API from '@aws-amplify/api'

function App() {
  console.log({ API })

  return (
    <div>lol</div>
  );
}

export default App;
